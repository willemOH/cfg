set from = "willemhilliard@gmail.com"
set realname = "Willem Hilliard"

# Imap settings
set imap_user = "willemhilliard@gmail.com"
set imap_pass = "<mutt-app-specific-password>"

# Smtp settings
set smtp_url = "smtps://willemhilliard@smtp.gmail.com"
set smtp_pass = "<mutt-app-specific-password>"

# Remote gmail folders
set folder = "imaps://imap.gmail.com/"
set spoolfile = "+INBOX"
set postponed = "+[Gmail]/Drafts"
set record = "+[Gmail]/Sent Mail"
set trash = "+[Gmail]/Trash"
